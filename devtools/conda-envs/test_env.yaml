name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Dependencies
  - numpy
  - math
    # Pip-only installs
  #- pip:
  #  - codecov

